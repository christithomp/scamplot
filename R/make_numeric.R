
make_numeric = function(data){
  p = ncol(data) # Get number of variables in data

  for (i in 1:p){
    if (!is.numeric(data[ , i]){
      if (is.character(data[ , i]) | is.string(data[ , i])){

      }
    }
  }

}
